refreshing json basics

nu oefeningen maken:
1. Save a dictionary — create a simple dictionary (like your person dict from Day 5), save it to a file called person.json using json.dump()
2. Load a dictionary — write a separate script that opens person.json and loads it back with json.load(), then prints it
3. Save a list of dictionaries — create a list with 2-3 workout dictionaries (like Day 5), save the whole list to workouts.json
4. Load the list — load workouts.json back and print each workout nicely formatted