#21/08/26
- Rewatched tutorial on functions
- made a greeting function where im able to reuse the function
- made calculator functions for add, subtract, multiply, divide. Each function takes 2 values and returns the result

#23/08/26
- function that prints when a number is even or odd
- List sum function — a function that takes a list of numbers and returns the total sum (use a loop inside)

#25/08/26
- little program with menu to choose between 2 options to add an exercise and vieuw options of the exercises added.